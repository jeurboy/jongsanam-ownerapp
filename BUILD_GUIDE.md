# Build Commands สำหรับ Jong Sanam Owner App

## การตั้งค่าก่อน Build

### 1. ตรวจสอบไฟล์ .env.production
ไฟล์นี้จะถูกใช้สำหรับ production build:
```
API_URL=https://api.jongsanam.online
ENV=production
```

### 2. สำหรับ iOS - แก้ไข exportOptions.plist
แก้ไขไฟล์ `ios/exportOptions.plist`:
- `YOUR_TEAM_ID`: ใส่ Team ID จาก Apple Developer Account
- `YOUR_PROVISIONING_PROFILE_NAME`: ใส่ชื่อ Provisioning Profile
- เปลี่ยน `method` เป็น:
  - `app-store`: สำหรับ App Store
  - `ad-hoc`: สำหรับ Ad-Hoc distribution
  - `enterprise`: สำหรับ Enterprise distribution
  - `development`: สำหรับ Development

#### ความแตกต่างระหว่าง Distribution Methods:

**📱 Ad-Hoc Distribution**
- **วัตถุประสงค์**: สำหรับทดสอบกับกลุ่มผู้ใช้จำกัด (Beta Testing)
- **จำนวนอุปกรณ์**: สูงสุด 100 เครื่องต่อปี
- **ต้องลงทะเบียน UDID**: ✅ ต้องลงทะเบียนทุกเครื่อง
- **ค่าใช้จ่าย**: $99/ปี (Apple Developer Account)
- **หมดอายุ**: Provisioning Profile หมดอายุใน 1 ปี
- **กรณีใช้งาน**: 
  - Beta Testing ก่อนปล่อย App Store
  - แจกให้ลูกค้า VIP ทดสอบ
  - Internal Testing กับทีมขนาดเล็ก
- **วิธีแจกจ่าย**: TestFlight (แนะนำ), ส่งไฟล์ IPA, MDM

**🏢 Enterprise Distribution**
- **วัตถุประสงค์**: สำหรับองค์กรขนาดใหญ่แจกจ่าย app ภายในบริษัท
- **จำนวนอุปกรณ์**: ไม่จำกัด
- **ต้องลงทะเบียน UDID**: ❌ ไม่ต้อง
- **ค่าใช้จ่าย**: $299/ปี (Apple Developer Enterprise Program)
- **คุณสมบัติองค์กร**: ต้องมีพนักงาน 100+ คน และมี D-U-N-S Number
- **ข้อจำกัด**: ⚠️ เฉพาะพนักงานในองค์กร - ห้ามแจกจ่ายสู่สาธารณะ
- **กรณีใช้งาน**:
  - App สำหรับพนักงานในบริษัทขนาดใหญ่
  - App ภายในโรงพยาบาล/ธนาคาร
  - ❌ ไม่เหมาะสำหรับแจกจ่ายให้ลูกค้าทั่วไป
- **วิธีแจกจ่าย**: Website ภายในองค์กร (OTA), MDM System

**📊 ตารางเปรียบเทียบ**

| คุณสมบัติ | Ad-Hoc | Enterprise |
|----------|---------|------------|
| จำนวนเครื่อง | สูงสุด 100 เครื่อง/ปี | ไม่จำกัด |
| ต้องลงทะเบียน UDID | ✅ ต้อง | ❌ ไม่ต้อง |
| ค่าใช้จ่าย | $99/ปี | $299/ปี |
| คุณสมบัติองค์กร | ไม่ต้องการ | ต้องมีพนักงาน 100+ คน |
| การใช้งาน | Testing, Beta | พนักงานภายในองค์กร |
| แจกจ่ายสาธารณะ | ได้ (จำกัด) | ❌ ห้าม (ผิดกฎ) |

**💡 คำแนะนำสำหรับ Jong Sanam Owner App:**
1. **App Store** (แนะนำที่สุด) - สำหรับให้เจ้าของสนามทั่วไปดาวน์โหลด
2. **Ad-Hoc + TestFlight** - สำหรับ Beta Testing กับกลุ่มทดสอบ 10-20 คน
3. **❌ ไม่แนะนำ Enterprise** - เพราะ app นี้ไม่ใช่สำหรับพนักงานภายในองค์กร

## Build Commands

### Android

#### 1. Build APK (สำหรับ testing)
```bash
npm run build:android:release
```
ไฟล์ APK จะอยู่ที่: `android/app/build/outputs/apk/release/app-release.apk`

#### 2. Build AAB (สำหรับ Google Play Store)
```bash
npm run build:android:bundle
```
ไฟล์ AAB จะอยู่ที่: `android/app/build/outputs/bundle/release/app-release.aab`

### iOS

#### 1. Build และติดตั้งบน Device (Release mode)
```bash
npm run build:ios:release
```

#### 2. Archive สำหรับ App Store
```bash
npm run build:ios:archive
```
Archive จะอยู่ที่: `ios/build/JongSanamOwnerApp.xcarchive`

#### 3. Export IPA
```bash
npm run build:ios:export
```
ไฟล์ IPA จะอยู่ที่: `ios/build/JongSanamOwnerApp.ipa`

#### 4. Archive + Export ในคำสั่งเดียว
```bash
npm run build:ios:archive && npm run build:ios:export
```

### Build ทั้งหมด (Android + iOS)
```bash
npm run build:all
```

## ขั้นตอนการ Build แบบเต็ม

### Android (Google Play Store)
1. ตรวจสอบ version ใน `android/app/build.gradle`
2. Build AAB: `npm run build:android:bundle`
3. Upload ไฟล์ AAB ไปยัง Google Play Console

### iOS (App Store)
1. ตรวจสอบ version ใน Xcode
2. แก้ไข `ios/exportOptions.plist` ให้ถูกต้อง
3. Archive: `npm run build:ios:archive`
4. Export: `npm run build:ios:export`
5. Upload IPA ไปยัง App Store Connect ด้วย Transporter หรือ Xcode

## Troubleshooting

### Android
- หาก build ล้มเหลว ให้ clean project ก่อน:
  ```bash
  cd android && ./gradlew clean && cd ..
  ```

### iOS
- ตรวจสอบว่า Xcode เปิดอยู่และ workspace ถูกต้อง
- ตรวจสอบ Signing & Capabilities ใน Xcode
- หาก archive ล้มเหลว ให้ลองเปิด Xcode และ archive ผ่าน GUI

## หมายเหตุ
- ทุก build command จะใช้ค่า environment variables จาก `.env.production`
- สำหรับ development ให้ใช้ `.env` ตามปกติ
- ตรวจสอบให้แน่ใจว่า `react-native-config` ได้ถูกติดตั้งและตั้งค่าถูกต้อง
